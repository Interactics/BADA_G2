# Signal ros node

This ROS node publishes recognized sound event.

## Installation

```shell
cd ~/catkin_make
catkin_make --only-pkg-with-deps signal_node
```

## Usage

```shell
rosrun signal_node signal_node.py
```